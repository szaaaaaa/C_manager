[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "rust_scanner"
version = "0.1.0"
description = "NTFS MFT scanner for C_manager"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
